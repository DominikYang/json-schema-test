name: 'push json to java repo'

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: clone target repo
      run: |
        pwd
        cd ..
        git clone https://github.com/DominikYang/java-demo-test.git
    - name: copy file to target directory
      run: |
        cd json-schema-test
        cp -b -r schema /home/runner/work/json-schema-test/java-demo-test/src/main/resources/schema
        cd ../java-demo-test/src/main/resources/schema
        ls
#    - run: |
#        pwd
#        ls -a






